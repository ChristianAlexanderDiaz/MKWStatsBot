discord.py>=2.0.0
numpy>=1.19.0
Pillow>=8.0.0
python-dotenv>=0.19.0
psycopg2-binary>=2.9.0
aiohttp>=3.8.0

# PaddleX direct integration for production deployment
paddlepaddle>=3.0.0
paddlex>=3.0.0

# PaddleX OCR dependencies (installed separately to ensure compatibility)
paddleocr>=3.0.0

# CRITICAL: Install headless OpenCV ONLY
# Note: nixpacks.toml handles removing GUI versions after PaddleX installation
opencv-contrib-python-headless>=4.5.0,<5.0.0

# Explicitly prevent GUI OpenCV installation (may not work with all pip versions)
# opencv-python==DONOTINSTALL
# opencv-contrib-python==DONOTINSTALL 