discord.py>=2.0.0
numpy>=1.19.0
Pillow>=8.0.0
python-dotenv>=0.19.0
psycopg2-binary>=2.9.0
aiohttp>=3.8.0

# PaddleX OCR - includes all OCR dependencies (paddlepaddle, paddleocr, etc.)
paddlex[ocr]>=3.0.0

# CRITICAL: Force headless OpenCV after PaddleX installation
# PaddleX may install GUI OpenCV, so we override it with headless version
opencv-contrib-python-headless>=4.5.0 