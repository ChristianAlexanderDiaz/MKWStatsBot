discord.py>=2.0.0
numpy>=1.19.0
Pillow>=8.0.0
python-dotenv>=0.19.0
psycopg2-binary>=2.9.0
aiohttp>=3.8.0

# PaddleX direct integration (Method 2 - no HTTP service needed)
paddlepaddle>=3.0.0
paddlex[ocr]>=3.0.0

# CRITICAL: Force headless OpenCV installation to prevent libGL.so.1 errors
# This prevents GUI dependency issues in Railway/Docker headless environments
opencv-contrib-python-headless>=4.5.0 