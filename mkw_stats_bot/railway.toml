[build]
builder = "DOCKERFILE"
# Remove this line if Dockerfile is in root, or update to correct path
# dockerfilePath = "Dockerfile"  # Not needed if in root

[deploy]
startCommand = "python main.py"
restartPolicyType = "ALWAYS"